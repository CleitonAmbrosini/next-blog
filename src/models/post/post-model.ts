export type PostModel = {
  id: string,
  title: string,
  slug: string,
  excerpt: string,
  content: string,
  converImageUrl: string,
  published: boolean,
  createAt: string,
  updateAt: string,
  author: string,
}